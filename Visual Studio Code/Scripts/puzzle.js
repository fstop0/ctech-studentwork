function setup() {
    createCanvas(windowWidth, windowHeight);
    background(35);
    
    //frame
    rect (10, 10, windowWidth-20, windowHeight-20);

    //pull images
    img = 
}

function draw() {
    
}